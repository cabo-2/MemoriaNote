namespace MemoriaNote
{
    /// <summary>Defines the persisted logging destination setting.</summary>
    public enum LoggerType
    {
        /// <summary>No configured logging destination.</summary>
        None,

        /// <summary>A file logging destination.</summary>
        File,

        /// <summary>A console logging destination.</summary>
        Console
    }
}
